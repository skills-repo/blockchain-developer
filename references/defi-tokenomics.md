# DeFi 收益与 Tokenomics 分析 Playbook

> 收益来自理解，不来自运气。本 playbook 给出结构化框架：先判断收益来源，再评估可持续性，最后量化风险。

## 决策树：一个 yield 机会该不该碰

```
看到某协议 APY 很高
  ├─ 收益来源清晰吗？(流动性激励 / 借贷利差 / 手续费 / 杠杆)
  │    ├─ 否 → 视为庞氏结构，回避
  │    └─ 是 → 进入可持续性评估
  ├─ 排放是否在衰减？(是) → 真实收益率 = 剔除代币排放后的净收益
  └─ 有无未披露解锁 / 团队 Allocation？(是) → 高风险，折价计入
```

## 收益来源四象限

| 来源 | 可持续性 | 风险点 |
|------|----------|--------|
| 交易手续费 | 高（真实使用） | TVL 波动、竞争 |
| 借贷利差 | 中高 | 坏账、清算连锁 |
| 流动性激励（代币排放） | 低（通胀） | 排放结束即崩 |
| 杠杆循环（loop） | 中 | 清算螺旋、脱锚 |

**真实收益率（Real Yield）** = 协议收入中分配给 LP/质押者的部分（剔除代币通胀）。永远用真实收益率而非名义 APY 做横向比较。

## Tokenomics 评估维度

1. **供应结构**：总量、流通量、通胀率、销毁机制。
2. **分配与解锁**：团队 / 投资人的 cliff 与线性解锁时间表；关注未来 90 天解锁 cliff。
3. **价值捕获**：代币是否捕获协议收入（回购销毁 / 分红 / 治理溢价）。
4. **集中度**：前 10 地址持币占比、LP 集中度。
5. **排放曲线**：排放速率是否衰减、是否有上限。

## 量化速算（确定性）

- **APY 换算**：`APY = (1 + APR/n)^n − 1`（n 为复利频次），`scripts/` 未内置时用手算核对展示口径。
- **解锁冲击**：`解锁额 / 日均成交量` > 0.3 视为显著抛压信号。
- **TVL/MCap 比**：< 0.1 通常意味着代币估值相对锁仓过高，脆弱。

## 输出模板

对每个 DeFi 标的产出一段话结论：收益来源 + 真实收益率 + 解锁风险 + 打分（1–5）。不替用户下注。

## 边界

- 不构成投资建议；只做框架化分析。
- 链上数据会滞后，结论需标注数据快照时间。

## 相关子技能与层次边界

- **落地动作（L3）**：需要跨协议收益对比、流动性分析、质押/再质押策略设计 → `skills/defi-yield/SKILL.md`；需要把 tokenomics 纳入一份完整项目研究 → `skills/crypto-report/SKILL.md`。
- **兄弟参考**：tokenomics 是 `references/onchain-research.md` 研究流水线第 2 阶段（定位 → Tokenomics → 链上验证 → 竞争 → 风险）；合约经济风险见 `references/smart-contract-security.md`。
- **层次边界**：本 playbook 只讲"收益来源判断 + 真实收益率 + 解锁冲击"的量化框架（决策层）；具体"扫哪些协议、怎么配仓位"由 `skills/defi-yield/` 负责，本文不重复其策略落地细节。
